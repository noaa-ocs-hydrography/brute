[DEFAULT]
# comma or newline (remember to indent) separated additional configs
additional_configs = nbs02.config, nbs_postgres.config, pbc18.config

tablenames = pbc_utm18n_mllw, pbc_utm18n_mllw_sensitive, pbc_utm18n_mllw_prereview
